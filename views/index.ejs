
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinematique</title>
    <link href="/styles/header.css" rel="stylesheet">
    <link href="/styles/index_body.css" rel="stylesheet">
</head>
<body>
    <div class="header">
        <div class="logo">
            <h1><a href="/">Cine<span style="color: blueviolet;font-size:medium;">matique</span></a></h1>
        </div>
        <div class="sec__part">
        <div class="search__bar">
            <form action="/search" method="post">
                <input type="text" name="search" placeholder=" search movie or tv..">
                <button type="submit"><a>Search</a></button>
            </form>
        </div>
        </div>
    </div>
    <div class="main">
    <% for (var i = 0; i < length ; i++) { 
     var item = content.results[i];
     var imageUrl = item.poster_path 
        ? "https://image.tmdb.org/t/p/w500" + item.poster_path 
       : "https://via.placeholder.com/500x750?text=No+Image";
     var title = item.title || item.name || "Untitled";
var mediaType = item.media_type || "movie"; // or pass it if known
  %>
    <a href="/card/<%= item.id %>?type=<%= mediaType %>" class="card">
      <div class="card__img">
        <img src="<%= imageUrl %>" alt="<%= title %>">
      </div>
      <div class="card__title">
        <h4><%= title %></h4>
      </div>
    </a>
<% } %>

    </div>
</body>
</html>