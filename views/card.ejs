<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinematique</title>
    <link href="/styles/card.css" rel="stylesheet">
</head>
<body style="
  background: url('https://image.tmdb.org/t/p/w780<%= card.backdrop_path %>') no-repeat center center;
  background-size: cover;
  background-attachment: fixed;
">

    <div class="card__main">
        <div class="part__1">
            <div class="poster"><img src="https://image.tmdb.org/t/p/w500<%=card.poster_path%>" alt="<%= card.title ||card.name|| 'untitled'%>"></div>
        </div>
        <div class="part__2">
            <div class="title">
                <h1><%= card.title || card.name %></h1>
            </div>
            <div class="rating">
                <h4 class="time"><%=card.runtime%> min</h4>
                <h4 class="release"><%=release_date%></h4>
                <div class="imdb"><h4 class="rating"><%=rating %></h4>
                    <div><img src="/img/imdb1.png" alt="imdb"></div></div>
                
            </div>
            <div class="genre">
                <h3>GENRES</h3>
                <div>
                  
                <% for(var i=0;i<card.genres.length;i++) {%>
                <h4><%= card.genres[i].name%></h4>
                <% } %>
              <% if(card.genres.length==0){%>
                    <h4>Drama</h4>
                    <% } %>
                </div>
            </div>
            <div class="description">
                <h3>SUMMARY</h3>
                <p><%= card.overview %></p>
            </div>
        </div>
    </div>
</body>
</html>
